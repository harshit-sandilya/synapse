package com.synapse.transport.common.enums;

public enum ArtifactType {
    DATASET_CONFIG,
    DATASET_CACHE,
    MODEL_CONFIG,
    MODEL_IR,
    TRAINING_METRICS,
    CHECKPOINT,
    INFERENCE_RESULT
}
